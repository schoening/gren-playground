port module Subscription exposing ( subscriptions, setCodeEditorValue )

import Message exposing (..)
import Model exposing (..)


port setCodeEditorValue : String -> Cmd msg


port onCodeEditorChanged : (String -> msg) -> Sub msg


subscriptions : Model -> Sub Msg
subscriptions model =
    onCodeEditorChanged OnCodeEditorChanged
